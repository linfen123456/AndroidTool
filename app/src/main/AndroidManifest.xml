<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidstutytool.androidtool">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".BottomMenu.BottomMenuActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".ViewPager.HelloActivity"
            android:label="启动欢迎页">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Exandablevlistview.GJCXFragment" />
        <activity android:name=".HTTP.httpRequest.getImage.GetHttpImageActivity" />
        <activity
            android:name=".HTTP.httpRequest.getHttpWEB.GetHttpResponeActivity"
            android:label="@string/title_activity_get_http_respone"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>